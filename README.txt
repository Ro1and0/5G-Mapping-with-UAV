In "lattepanda Mapping 5G" there is the code that reads the signals from the embedded 5G modem. 

-"Node on lattepanda.sh".  structure of the important files in the lattepanda 
-5GCS_log.servce. Is the service that runs automatically 
-\lattepanda Mapping 5G\opt\5GCSlog. Main code, this is called by the service during the start up.
-\lattepanda Mapping 5G\opt\5GCSlog\logs. Example output file generated by the code (for post experiments analysis).
